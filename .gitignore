# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Internal documents (keep local, not for public repo)
# Only docs/screenshots/ is tracked for README images
docs/*
!docs/screenshots/
# docs/architecture.md is referenced by CLAUDE.md — keep it in the repo
!docs/architecture.md
# referenced by ecosystem_deep_reviewer.py dispatch prompt — hook 段位映射的真相源
!docs/ecosystem-deep-review-template.md
# referenced by CLAUDE.md — 记忆系统 v2 规格（memory_add/reconcile 的唯一规格来源）
!docs/memory-v2-design.md
# 工具渐进式加载 P1/P2/P3 唯一规格来源（CHANGELOG 声明 checked in）
!docs/tool-loading-design.md
# 分发改造 P1 的 Windows 设备验证 runbook——须随仓库到另一设备实测
!docs/dist-refactor-p1-windows-verification.md

# Node (Dashboard)
dashboard/node_modules/
# dashboard/dist/ is a dev build artifact (ignored via dist/ above);
# plugin/dashboard-dist/ is the committed copy for marketplace users
dashboard/.next/

# Database
*.db
*.sqlite3

# Docker
docker-compose.override.yml

# Testing
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/

# Linter cache
.ruff_cache/

# Temp
*.tmp
*.log
tmp_*
tempnpm-cache/
dashboard/tempnpm-cache/
test-screenshots/
aiteam.db.migrated

# MCP config (generated per-user, see .mcp.json.example)
.mcp.json

# Claude settings (generated per-user, see .claude/settings.json.example)
.claude/settings.json
.claude/settings.local.json
scripts/sandbox_test.py
scripts/hn_monitor.py
scripts/.hn_monitor_state.json
.audit_findings.md
.audit_findings_phase2.md
.fix_context_resolve_and_hook_translator.md
pip/

# Performance benchmark outputs (not for repo)
tmp/
bench-*.json

# SQLite artifacts (runtime DB lives in ~/.claude/data)
aiteam.db
*.db-shm
*.db-wal

# Codex CLI 过程文件（2026-07-27）：.codex/ 是 Codex 的项目配置目录，
# AGENTS.md 是其读取的 CLAUDE.md 等价副本——均为本机开发过程产物，非 plugin 产品面。
# 注意与产品本体区分：plugin/**、.claude/agents/** 是交付物，已刻意入库。
.codex/
/AGENTS.md
